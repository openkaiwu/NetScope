<UserControl x:Class="NetScope.App.Views.DiagnosticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:NetScope.App.Views">
    <Grid Margin="24,18,24,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,14">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="网络工具" FontSize="26" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <Ellipse Width="8" Height="8" Fill="{DynamicResource SuccessBrush}" Margin="0,0,7,0"/>
                    <TextBlock Text="{Binding NetworkName}" FontWeight="SemiBold"/>
                    <TextBlock Text="  ·  "/>
                    <TextBlock Text="{Binding NetworkStatus}" Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>
            <Border Grid.Column="1" VerticalAlignment="Center" Padding="10,5" CornerRadius="12"
                    Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="{StaticResource FluentIcons}" Text="&#xE8B7;" Foreground="{DynamicResource SuccessBrush}" Margin="0,0,6,0"/>
                    <TextBlock Text="只读 · 本地处理" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="1" HorizontalAlignment="Left" Background="{DynamicResource SurfaceAltBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="4" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding ShowNetworkDiagnosticCommand}" Padding="18,8">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsNetworkDiagnosticSelected}" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource FluentIcons}" Text="&#xE9D9;" Margin="0,0,8,0"/>
                        <TextBlock Text="网络诊断" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ShowSpeedTestCommand}" Padding="18,8">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSpeedTestSelected}" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource FluentIcons}" Text="&#xE9D2;" Margin="0,0,8,0"/>
                        <TextBlock Text="网速测试" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <views:NetworkDiagnosticView Visibility="{Binding IsNetworkDiagnosticSelected, Converter={StaticResource BoolToVisibility}}"/>
            <views:SpeedTestView Visibility="{Binding IsSpeedTestSelected, Converter={StaticResource BoolToVisibility}}"/>
        </Grid>
    </Grid>
</UserControl>
